apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: demo-app-workflow

on:
  workflow_dispatch:

jobs: 
  pass:
    steps:
      - name: Deploy status
        uses: docker://alpine:3.20
        run: echo "success"
