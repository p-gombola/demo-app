apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: demo-app-workflow

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: github.com/p-gombola/shared/.cloudbees/workflows/deploy.yaml@main
  
